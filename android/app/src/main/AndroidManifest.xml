<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- AnkiDroid's read/write API permission (defined by AnkiDroid, granted at runtime). -->
    <uses-permission android:name="com.ichi2.anki.permission.READ_WRITE_DATABASE" />

    <!-- Edge neural TTS synthesis (cached on disk; offline falls back to device TTS). -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Car mode: spoken commands between TTS playbacks. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- M2 screen-off session: foreground media service + lock-screen controls. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Android 11+ package visibility: without this our ContentResolver cannot see
         AnkiDroid's provider and queries silently return null. -->
    <queries>
        <provider android:authorities="com.ichi2.anki.flashcards" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnkiAudio">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".TouchStudyActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".GestureChartActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <service
            android:name=".StudyService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback|microphone" />
    </application>

</manifest>